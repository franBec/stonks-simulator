spring:
  application:
    name: stonks_java

management:
  endpoints:
    web:
      exposure:
        include: modulith

stonks:
  cobol:
    programs:
      trade-validator:
        path: "../stonks_cobol/bin/trade-validator"
        timeout-seconds: 5
