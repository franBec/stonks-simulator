spring:
  application:
    name: stonks_java
  ai:
    openai:
      base-url: https://openrouter.ai/api/v1
      api-key: ${OPENROUTER_API_KEY}
      chat:
        options:
          model: openrouter/free
  sql:
    init:
      mode: always
      schema-locations: classpath:stonks-schema.sql
      data-locations: classpath:stonks-data.sql
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: false
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=500, expireAfterWrite=60s

management:
  endpoints:
    web:
      exposure:
        include: modulith
  otlp:
    metrics:
      export:
        enabled: ${stonks.adapters.otel:false}

stonks:
  adapters:
    db: h2
    cobol: stub
    ai: stub
    news: stub
    otel: false
  cobol:
    programs:
      trade-validator:
        path: "../stonks_cobol/bin/trade-validator"
        timeout-seconds: 5
      price-engine:
        path: "../stonks_cobol/bin/price-engine"
        timeout-seconds: 5
      catalog:
        path: "../stonks_cobol/bin/catalog"
        timeout-seconds: 5
      portfolio-mgr:
        path: "../stonks_cobol/bin/portfolio-mgr"
        timeout-seconds: 5
  market:
    simulation:
      interval-ms: 5000
  broadcast:
    sse-timeout-ms: 300000
    heartbeat-rate-ms: 15000
    trade-paper-tape-format: "TRADE | %s %d %s @ $%.2f | TOTAL: $%.2f"
    paper-tape-entry-format: "TRADE #%04d | %s %d %s @ $%.2f | TOTAL: $%.2f"
  news:
    rss:
      feed-urls:
        - https://feeds.bbci.co.uk/news/technology/rss.xml
        - https://techcrunch.com/feed/
        - https://www.theguardian.com/uk/business/rss
  chaos:
    enabled: true
    event-check-interval-ms: 10000
  logging:
    max-collection-print-size: 3
    adapter-out:
      exclude-patterns: "StockPriceEngineCobolAdapter"
